const API_URL = "http://localhost:3000/api/v1"; // Cambia la URL según corresponda

export default {
  apiUrl: API_URL,
  usersEndpoint: `${API_URL}/users`,
  documentsEndpoint: `${API_URL}/document`,
};
